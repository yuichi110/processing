/*
void network_test_settings(){
 size(1800, 1000); 
}

void network_test_setup(){
  background(255); 
}

void network_test_draw(){
  draw2wayBigArrow(300, 300, 400, 200, 100, 50, 0, "", 2, 255, "concrete", 255);
}

void network_test_BriefFormats(){
  // L2
  drawEth(100, 100, "concrete", "concrete", false);
  drawEth(100, 200, "concrete", "concrete", true);
  
  drawEthIp(100, 300, "concrete", "concrete", "concrete", false);
  drawEthIp(100, 400, "concrete", "concrete", "concrete", true);
  
  drawEthIpTransport(100, 500, "concrete", "concrete", "concrete", "concrete", false, false);
  drawEthIpTransport(100, 600, "concrete", "concrete", "concrete", "concrete", true, true);
  
  // L3
  drawIp(500, 100, "concrete", "concrete", false);
  drawIp(500, 200, "concrete", "concrete", true);
  
  drawIpTransport(500, 300, "concrete", "concrete", "concrete", false, false);
  drawIpTransport(500, 400,  "concrete", "concrete", "concrete", true, true); 
  
  // L4
  drawTransport(1000, 100, "concrete", "concrete", false, false);
  drawTransport(1000, 200, "concrete", "concrete", true, true); 
  
  // L7
  drawData(1500, 100,  "concrete"); 
}

void testFocusFormats(){
  drawEthDetail(100, 100, "concrete", "concrete", "0000.0000.0101", "0000.0000.0102", false);
}
*/